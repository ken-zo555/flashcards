<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="apple-touch-icon" sizes="76x76" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" href="/favicon.png">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
    <meta content='width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0, shrink-to-fit=no' name='viewport' />

    <title>Flashcards</title>
    <%= csrf_meta_tags %>
    <!-- Fonts and icons -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Montserrat:400,700,200" />
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/latest/css/font-awesome.min.css" />
    <!-- CSS Files -->
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <style>
      @font-face {
          font-family: 'Nucleo Outline';
          src: url(<%= asset_path "nucleo-outline.eot" %>);
          src: url(<%= asset_path "nucleo-outline.eot" %>) format("embedded-opentype"), url(<%= asset_path "nucleo-outline.woff2" %>) format("woff2"), url(<%= asset_path "nucleo-outline.woff" %>) format("woff"), url(<%= asset_path "nucleo-outline.ttf" %>) format("truetype");
          font-weight: normal;
          font-style: normal;
      }
    </style>
    <!--   Core JS Files   -->
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="index-page sidebar-collapse">
    <%= render 'layouts/navbar' %>
    <div class="wrapper">
      <div class="page-header clear-filter" filter-color="orange" style="margin-top: 57px">
        <div class="page-header-image" data-parallax="true" style="background-image: url(<%= asset_path "header_1.jpg" %>); margin-top: 0px">
        </div>
          <div class="container">
            <div align="left"><%= breadcrumbs pretext: " ", separator: " &rsaquo; " %></div>
            <%= render 'layouts/flash_messages' %>
            <%= yield %>
            <%= render 'layouts/footer' %>
          </div>
      </div>
    </div>
  </body>
</html>
